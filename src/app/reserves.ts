import { Time } from '@angular/common';

export class Reserves{

  idReserva?: number;
  idUsuario?: number;
  unidade?: string;
  qtdPessoas?: number;
  data?: Date;
  hora?: Time;
  status?: string;
}
